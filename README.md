# SwiftPDF Studio

Browser-first PDF workspace with live preview, stitch, fill/sign helpers, and guided controls.

## GitHub Pages

Use `index.html` as the entry point.

If GitHub Pages is not enabled yet:
1. Open repo settings.
2. Go to Pages.
3. Set source to `Deploy from branch`.
4. Select `main` and `/(root)`.
